# Binary files - mark as binary and exclude from exports
*.png binary -export-ignore
*.ico binary -export-ignore
*.jpg binary -export-ignore
*.jpeg binary -export-ignore
*.gif binary -export-ignore
*.svg binary -export-ignore
*.webp binary -export-ignore
*.exe binary -export-ignore
*.dll binary -export-ignore
*.so binary -export-ignore
*.dylib binary -export-ignore

# Handle line endings automatically for files detected as text
# and leave all files detected as binary untouched.
* text=auto eol=lf

# Force bash scripts to always use LF line endings so that if a repo is accessed
# in Unix via a file share from Windows, the scripts will work.
*.sh text eol=lf
